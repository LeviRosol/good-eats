# Gemfile
source "https://rubygems.org"

# Use the GitHub Pages gem, which pins Jekyll and supported plugins
gem "github-pages", group: :jekyll_plugins

# Local dev helpers
gem "webrick", group: :jekyll_plugins   # needed for Ruby 3+

gem "minimal-mistakes-jekyll"
